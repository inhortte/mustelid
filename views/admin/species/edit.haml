%h3
  Update Species

%p
  Subfamily:
  %span#subfam
    %select{:id => 'subfam_select', :name => 'subfam_select'}
      = options_for_select(@model.gen.subfam.id, get_subfamilies)
  Genus:
  %span#gen
    = haml_partial "partials/_genus", :locals => { :subfam_id => @model.gen.subfam.id, :gen_id => @model.gen.id }

= haml_partial "admin/partials/_new_edit", :locals => { :link => @link, :var => @var, :model => @model }

%p
  Common Names(comma separated list)
  %br
  %textarea#common{:cols => 40, :rows => 3}
    = @model.druh_common_names.map { |cn| cn.name }.join(", ")
